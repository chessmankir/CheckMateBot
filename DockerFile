FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .
CMD ["node", "index.js"]
# НИ ОДНОГО ENV/ARG с токенами здесь нет. Все секреты приходят через `docker run -e …`/панель.
