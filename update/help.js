// modules/cmd.help.js

const isAdminChat = require('../admin/permissionAdminChat');

module.exports = function (bot) {
  bot.onText(/^!команды$/i, async (msg) => {
    const chatId = msg.chat.id;

    const isAdminChatPermission = await isAdminChat(chatId);

    if (isAdminChatPermission) {
      // ===== Панель администратора =====
      const adminText = `
🛡️ *Панель администратора — команды*

*Изменение описания (по тегу):*
• \`+ник @тегИгрока новыйНик\` — меняет ник заданного игрока.  
• \`+возраст @тегИгрока 21\` — меняет возраст заданного игрока.  
• \`+город @тегИгрока Город\` — меняет город заданного игрока.  
• \`+имя @тегИгрока Имя\` — меняет имя заданного игрока.  
• \`+id @тегИгрока 123456789\` — меняет PUBG ID заданного игрока.

*Кланы и слоты:*
• \`!инвайтN\` — получить инвайт-код в клан №*N*.  
• \`!места\` — показать лимиты и свободные места по подкланам текущего клана.  
• \`!списокN\` — список сокланов клана №*N*.  
• \`+подклан @тегНовогоЛидера\` — создать подклан и назначить лидера.  
• \`+ссылка N https://t.me/...\` — установить ссылку чата клана №*N*.  
• \`+лимит N M\` — установить лимит *M* для клана №*N*.

*Модерация:*
• \`!бан @тегИгрока\` — забанить игрока во всех чатах клана.  
• \`!разбан @тегИгрока\` — снять бан.  
• \`!баны\` — список забаненных.

*Статистика:*
• \`!активность @тегИгрока\` — активность заданного игрока.

*Чаты:*
• \`!привязать чат\` — привязать текущий чат к клану  
  _(может только создатель, который регистрировал клан)_.

*Правила:*
• \`+правила\` (в сообщении ниже — сам текст) — устанавливает/обновляет правила клана.
      `.trim();

      return bot.sendMessage(chatId, adminText, { parse_mode: 'Markdown' });
    }

    // ===== Обычные команды для участников =====
    const userText = `
📖 *Список доступных команд:*

▫️ *описание* — ваше описание.  
В ответ на чужое сообщение — описание того, кому вы отвечаете.  
▫️ *описание @тег* — описание человека по тегу.

✏️ *Изменение описания (для себя):*  
• \`+ник\` — меняет ваш ник.  
• \`+возраст\` — меняет ваш возраст.  
• \`+город\` — меняет ваш город.  
• \`+имя\` — меняет ваше имя (если заполнено неправильно).

💍 *Браки:*  
• \`!браки\` — список браков текущего чата.  
• \`!брак @тег\` — предложение вступления в брак.  
• \`!развод\` — расторжение отношений.

📜 *Клан:*  
• \`правила\` — правила клана.  
• \`!земляк\` — сокланы в радиусе 400 км от вашего города.
    `.trim();

    await bot.sendMessage(chatId, userText, { parse_mode: 'Markdown' });
  });
};
